{
  "name": "astra-watcher",
  "version": "1.0.0",
  "description": "Voice memo file watcher and copier daemon",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build --compile --outfile=dist/astra-watcher src/index.ts",
    "watch": "bun run --watch src/index.ts",
    "dev": "bun src/index.ts"
  },
  "dependencies": {
    "chokidar": "^3.5.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}